const isValidId = require("./isValidId/isValidId");
const notValidBody = require("./notValidBody/notValidBody");
const { validateCode } = require("./validateCode/validateCode");

module.exports = { isValidId, notValidBody, validateCode };
